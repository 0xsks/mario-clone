// Define a variable to track points
let points = 0;

// Function to increment points
function incrementPoints() {
    points++;
}

// Function to decrement points
function decrementPoints() {
    points--;
}
